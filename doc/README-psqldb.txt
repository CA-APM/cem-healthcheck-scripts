The PSQL run will collect all information suitable to identify issues
with the PostgreSQL DB as used in an APM 9.x installation.  The
required Database credentials will be asked prior execution of the
script itself.
